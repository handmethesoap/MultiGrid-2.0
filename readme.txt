 -DUSEOMP -fopenmp : Add as a compile option in makefile to enable parallelization
-DTEST : Add as a compile option in makefile to enable output of multigrid statistics

Run: 
./multigrid number_of_levels number_of_threads

